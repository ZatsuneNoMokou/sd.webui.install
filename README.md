# Easy stable diffusion install

Just Windows currently (require [Chocolatey](https://chocolatey.org/install))

1. Install `python3.10` and `git` with `Chocolatey`
````batch
choco install python310 git
````

2. Run `install.bat`

2. Run `run.bat`